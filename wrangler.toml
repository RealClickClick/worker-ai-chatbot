name = "ai-telegram-bot"
main = "src/index.ts"
compatibility_date = "2026-06-24"
compatibility_flags = ["streams_enable_constructors"]

# D1 Database for chat history and user settings
[[d1_databases]]
binding = "DB"
database_name = "ai-telegram-bot-db"
database_id = "your-d1-database-id-here"

# AI binding for Cloudflare Workers AI
[ai]
binding = "AI"

# Public environment variables (non-sensitive, can be hardcoded)
[vars]
WORKER_DOMAIN = "your-worker.your-subdomain.workers.dev"

# Set ALL secrets via `npx wrangler secret put <NAME>`:
#   TELEGRAM_BOT_TOKEN  — Bot token from @BotFather (required)
#   WEBHOOK_SECRET      — Secret token for webhook verification (recommended)
#   BRAVE_API_KEY       — API key for /search via Brave Search (optional)
#   GOOGLE_GEMINI_API_KEY — API key for Gemini models (optional)
#   BOT_USERNAME        — Bot username WITHOUT @ (e.g. "MyBot") for group mention detection
#   BOT_NAME            — Custom bot display name (optional)
#   BOT_DESCRIPTION     — Custom bot description injected into system prompt (optional)
#   ADMIN_IDS            — Comma-separated Telegram user IDs for admin access

# Cron triggers:
#   0 7 * * *    — Daily tips (7:00 AM UTC)
#   */1 * * * * — Process due reminders (every minute)
[triggers]
crons = ["0 7 * * *", "*/1 * * * *"]
